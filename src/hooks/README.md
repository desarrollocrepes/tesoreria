# Hooks

Custom hooks de React para lógica reutilizable.

## Ejemplos:
- `useAutenticacion.js` - Hook para manejar autenticación
- `useFormulario.js` - Hook para validación de formularios
- `useCaja.js` - Hook para operaciones de caja
